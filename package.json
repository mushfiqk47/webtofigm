{
  "name": "html-to-figma",
  "version": "2.0.0",
  "description": "Production-grade HTML to Figma conversion plugin",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run build:sandbox -- --watch\" \"npm run build:ui -- --watch\" \"npm run build:extension -- --watch\"",
    "build": "npm run build:sandbox && npm run build:ui && npm run build:extension",
    "build:sandbox": "esbuild src/sandbox/code.ts --bundle --outfile=dist/code.js --target=es2020 --format=iife --banner:js=\"// Figma Plugin\"",
    "build:ui": "esbuild src/ui/ui.ts --bundle --outfile=dist/ui.js --target=es2020 --format=iife && node scripts/build-ui.js",
    "build:extension": "esbuild chrome-extension/content-script.ts --bundle --outfile=chrome-extension/content-script.js --target=es2020 --format=iife",
    "build:all": "npm run build",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/core": "^7.28.5",
    "@babel/preset-env": "^7.28.5",
    "@figma/plugin-typings": "^1.98.0",
    "@types/chrome": "^0.1.32",
    "@types/jest": "^30.0.0",
    "@types/jsdom": "^27.0.0",
    "babel-jest": "^30.2.0",
    "concurrently": "^8.2.2",
    "esbuild": "^0.20.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "jsdom": "^27.3.0",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
